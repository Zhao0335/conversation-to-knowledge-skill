# Tests

The GitHub Actions workflow currently performs repository smoke checks by compiling the bundled Python helpers and validating the worked Markdown example. Semantic behavior is specified separately in the skill's quality rubric and examples.
